This program excersizes basic networking principles in C++ that connects a client and a server together, and sends messages to one another.
The messages are sent via a socket stream and a message buffer
